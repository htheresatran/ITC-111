var str = 'this is a string';

// length method - returns size of the string
var strLen = str.length;

// square brackets notation - tring go to character at specificied string
var strIdx = str[9];

// indexOf method - get position of character in string
var strIdcOf = str.indexOf('r');

// slice method - get a substring or specified portion of a string.center
var strSlice = str.slice(5, 7); // should pull out is

// to UpperCase() and to LowerCase() - change the case of a string
var strUpper = str.toUpperCase();
var strLower = str.toLowerCase();

// replace() method - replaces characters of a string
var strRepl = str.replace('a', 'our'); //replaces a with our


//console.log(strUpper);

//number methods

var num = 75;

console.log(num.isInteger);

// Arrays

var fruits = ['apples', 'oranges', 'grapes', 'lemons', 'bananas']

var fruits = [];
fruits [0] = 'apples';

console.log(fruits[2]);

// array methods

//length method - gets number of elements
//console.log(fuirts.length);

//push() - adds new element to end of array
    //console.log(fruits.push('cantelope'));
    //fruits.pop('cantelop');

// sort() method - sorts the elements in the array
//console.log(fruits.sort());

// toString() method - converts an array to a string and returns result
    console.log(fruits);
    console.log(fruits.toString());
    










